# pnpm node linker: hoisted 방식 사용
# 이유: Vite/Vitest/ESLint 설정 파일들이 transitive 패키지(@eslint/js 등)를
#       직접 require하는 구조로, pnpm 기본 isolated linker로는 resolve 실패.
#       프로토타입 단계에서 DX 우선, 추후 isolated로 전환 시 direct dep 명시 필요.
node-linker=hoisted
shamefully-hoist=true
